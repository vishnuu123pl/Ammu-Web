// This component is kept as a no-op stub to avoid breaking imports in ProfilePage
// Location is now a manually entered text field, not GPS-based
export default function UpdateItemLocationButton(_props: { itemId: bigint }) {
  return null;
}
